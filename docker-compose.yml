version: '3.1'

services:
  db:
    image: postgres:9.4
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"   

  backend:
    build: ./back
    ports:
      - 8080:8080   

  # frontend:
  #     build: ./web
  #     ports:
  #       - 5000:80
    


